{
  "entities": {
    "Admin": {
      "title": "Admin",
      "description": "System administrator with manual page management privileges.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "Admin email coordinate"
        },
        "is_admin": {
          "type": "boolean",
          "description": "Flag indicating administrative authorization"
        }
      },
      "required": ["email", "is_admin"]
    },
    "PaginaRomantica": {
      "title": "PaginaRomantica",
      "description": "A customized persistent romantic page created manually by an admin or a user.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique UUID identifier"
        },
        "dados": {
          "type": "object",
          "description": "Nested configuration block matching LovePageConfig shape"
        },
        "criado_em": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp representing the creation moment"
        },
        "created_by": {
          "type": "string",
          "description": "User UID of creator"
        }
      },
      "required": ["id", "dados", "criado_em", "created_by"]
    }
  },
  "firestore": {
    "admins/{adminId}": {
      "schema": "Admin",
      "description": "Administrative users permitted to see and manage all user-generated pages."
    },
    "paginas/{paginaId}": {
      "schema": "PaginaRomantica",
      "description": "Client romantic pages saved persistently."
    }
  }
}
